export enum SIGN_FORM {
	USERNAME = 'username',
	PASSWORD = 'password',
	PASSWORD_CONFIRMATION = 'passwordConfirmation',
}

export enum USERNAME_LENGTH {
	MIN = 2,
	MAX = 21,
}

export enum PASSWORD_LENGTH {
	MIN = 5,
}

export enum RSS_FORM {
	URL = 'URL',
}
